# python
python interpreter is a binary which can be executed directly by CPU.
There are two components in interpreter: compiler and pvm, the former transform .py text file into byte code which is binary code executed by pvm rather processor. The pvm then transform the binary code to other bianry code which can be executed directly by processor.
